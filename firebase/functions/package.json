{
  "name": "functions",
  "scripts": {
    "//": "build workaround: https://github.com/microsoft/TypeScript/issues/13992#issuecomment-386259930",
    "build": "if [ -d ../../node_modules ]; then mv ../../node_modules ../../node_modules_tmp; fi; tsc src/*.ts; if [ -d ../../node_modules_tmp ]; then mv ../../node_modules_tmp ../../node_modules; fi",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "14"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^9.8.0",
    "firebase-functions": "^3.14.1"
  },
  "devDependencies": {
    "typescript": "^3.8.0",
    "firebase-functions-test": "^0.2.0"
  },
  "private": true
}
